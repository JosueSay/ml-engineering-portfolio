# Dependencias del experimento de vision, no del paquete.
#
# Estaban en la raiz del caso de estudio, donde parecian ser del pipeline: son
# de este experimento y solo de el. El pipeline productivo no necesita torch ni
# transformers, y arrastrarlos habria pesado en cada instalacion del paquete.
#
# Se fijan por version exacta a proposito: este experimento existe para poder
# reproducir lo que se probo, no para correr con lo ultimo de cada biblioteca.
#
#   python -m venv .venv && .venv/bin/pip install -r requirements.txt

numpy==2.5.1
PyYAML==6.0.3
torch==2.9.1
transformers==5.10.1
pillow==11.3.0
pillow-heif==1.5.0
einops==0.8.2
timm==1.0.28
